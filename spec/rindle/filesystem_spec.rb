require "spec_helper"

describe Rindle::Filesystem do
  context 'when mounted' do
    context 'browsing /collections' do
      it 'shows the collections loaded'
      it 'shows files, that weren\'t associated with a collection'
    end
  end
end
